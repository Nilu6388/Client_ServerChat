# Chat Application Using Python Socket Programming

## 📌 Objective
A simple chat application that allows two users to send messages between client and server using TCP/IP socket programming.

## ⚙️ Features
- Real-time text communication
- Based on TCP protocol
- Client–Server architecture

## 🧠 Concept
The server listens for incoming connections, and the client connects to the server. Once connected, both can exchange messages until one exits.

## 🧩 Tools Used
- Python 3
- `socket` library



